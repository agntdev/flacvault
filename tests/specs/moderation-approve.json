[
  {
    "name": "Approve button rejects non-admin",
    "steps": [
      {
        "send": { "callback": "moderation:approve:1" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Only admins can approve submissions."
            }
          }
        ]
      }
    ]
  }
]
