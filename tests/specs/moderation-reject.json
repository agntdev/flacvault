[
  {
    "name": "Reject button rejects non-admin",
    "steps": [
      {
        "send": { "callback": "moderation:reject:1" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Only admins can reject submissions."
            }
          }
        ]
      }
    ]
  }
]
