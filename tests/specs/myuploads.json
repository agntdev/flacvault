[
  {
    "name": "/myuploads shows empty state when user has no uploads",
    "steps": [
      {
        "send": { "text": "/myuploads" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "You haven't uploaded any tracks yet. Tap Submit Track to add one."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "My uploads button shows user's submissions",
    "steps": [
      {
        "send": { "callback": "myuploads:show" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "You haven't uploaded any tracks yet. Tap Submit Track to add one."
            }
          }
        ]
      }
    ]
  }
]
